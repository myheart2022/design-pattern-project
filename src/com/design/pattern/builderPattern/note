建造者模式重点在于使用一个辅助类建造者将复杂的类的组成分别创建，再将其聚合成一个大的对象
如示例中电脑配件的创建，调用者直接使用创建者去创建这些内部对象，达到解耦的目的
如果被创建的对象有很多相同的属性，也可以增加一个指挥类Director对公共代码进行提炼
对于建造者中建造方法，可以让其返回建造者本身，这样就可以使用链式调用以减少代码量
如StringBuilder和Lombok中的建造者模式。